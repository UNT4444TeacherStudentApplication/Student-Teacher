<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="myapplication.whatisthis.com.student_teacher">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainLanding">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CourseDocuments" />
        <activity android:name=".Calendar" />
        <activity android:name=".Grades" />
        <activity android:name=".Messaging" />
        <activity android:name=".CourseLanding" />
        <activity android:name=".Login" />
        <activity android:name=".CreateLogin"></activity>
    </application>

</manifest>